import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'JeferSetiawan_00000059297_IF670_AL_UTS',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
